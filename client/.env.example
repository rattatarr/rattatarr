# API Configuration
# The base URL for the Rattatarr backend API (used in production builds)
# In development, the Vite dev server proxies API requests automatically
VITE_API_BASE_URL=http://127.0.0.1:8080

# Backend server for Vite proxy (development only)
VITE_BASE_SERVER_URL=127.0.0.1:8080
